// 強風エフェクト（雨なし）

function drawKyoufuu() {
    // やや暗めのグレー背景（雲が多い感じ）
    setGradient(0, 0, width, height,
        color(160, 160, 160), color(200, 200, 200));

    // 風のみ（パーティクルなし、または将来的に軽いパーティクル追加可能）
    // 現時点では背景のみで風を表現
}
